set( PROJECT "srtm" )
set( TARGET_NAME snark_terrain_dem_${PROJECT} )
file( GLOB source *.cpp )
file( GLOB includes *.h )
source_group( ${TARGET_NAME} FILES ${source} ${includes} )
add_library( ${TARGET_NAME} OBJECT ${source} ${includes} )
install( FILES ${includes} DESTINATION ${snark_INSTALL_INCLUDE_DIR}/terrain/dem/${PROJECT} )
